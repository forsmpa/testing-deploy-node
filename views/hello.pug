extends layout

block content
	<!DOCTYPE html>
	html(lang="en")
	head
		meta(charset="UTF-8")
		title Document
	body



	.wrapper.ohonoooooooo
		- const upDog = dog.toUpperCase();
		p.hello Hello my dogs name is #{dog} or #{dog2} or #{upDog}
		span#yo Yo!!!
		img(src="dog.jpg", alt=`Dog ${dog2}`)

	h2 
		| Hello
		em How are you?

	h2 Sale ends in #{h.moment().endOf('day').fromNow()}